# idcd 生产环境 Docker Compose
# 部署路径: /opt/idcd/docker-compose.yml
# 数据库 / Redis 使用云托管服务，不在本文件中定义
#
# 启动:   docker compose up -d
# 查日志: docker compose logs -f api
# 依赖:
#   - /opt/idcd/config/prod.env.yaml  (包含所有密钥，不入 git)
#   - .env 文件包含 GHCR_OWNER 变量

services:
  api:
    image: ghcr.io/${GHCR_OWNER}/idcd-api:latest
    container_name: idcd-api
    restart: unless-stopped
    ports:
      - "127.0.0.1:8080:8080"
    volumes:
      - /opt/idcd/config/prod.env.yaml:/config/prod.env.yaml:ro
    command: ["/bin/api", "-config", "/config/prod.env.yaml"]
    environment:
      - TZ=Asia/Shanghai
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8080/health"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 15s
    logging:
      driver: "json-file"
      options: {max-size: "100m", max-file: "5"}

  aggregator:
    image: ghcr.io/${GHCR_OWNER}/idcd-aggregator:latest
    container_name: idcd-aggregator
    restart: unless-stopped
    volumes:
      - /opt/idcd/config/prod.env.yaml:/config/prod.env.yaml:ro
    command: ["/bin/aggregator", "-config", "/config/prod.env.yaml"]
    environment:
      - TZ=Asia/Shanghai
    depends_on:
      api:
        condition: service_healthy
    logging:
      driver: "json-file"
      options: {max-size: "50m", max-file: "3"}

  notifier:
    image: ghcr.io/${GHCR_OWNER}/idcd-notifier:latest
    container_name: idcd-notifier
    restart: unless-stopped
    volumes:
      - /opt/idcd/config/prod.env.yaml:/config/prod.env.yaml:ro
    command: ["/bin/notifier", "-config", "/config/prod.env.yaml"]
    environment:
      - TZ=Asia/Shanghai
    logging:
      driver: "json-file"
      options: {max-size: "50m", max-file: "3"}

  gateway:
    image: ghcr.io/${GHCR_OWNER}/idcd-gateway:latest
    container_name: idcd-gateway
    restart: unless-stopped
    ports:
      - "127.0.0.1:8443:8443"
    volumes:
      - /opt/idcd/config/prod.env.yaml:/config/prod.env.yaml:ro
      - /opt/idcd/certs:/certs:ro
    command: ["/bin/gateway", "-config", "/config/prod.env.yaml"]
    environment:
      - TZ=Asia/Shanghai
    logging:
      driver: "json-file"
      options: {max-size: "50m", max-file: "3"}

  scheduler:
    image: ghcr.io/${GHCR_OWNER}/idcd-scheduler:latest
    container_name: idcd-scheduler
    restart: unless-stopped
    volumes:
      - /opt/idcd/config/prod.env.yaml:/config/prod.env.yaml:ro
    command: ["/bin/scheduler", "-config", "/config/prod.env.yaml"]
    environment:
      - TZ=Asia/Shanghai
    depends_on:
      api:
        condition: service_healthy
    logging:
      driver: "json-file"
      options: {max-size: "50m", max-file: "3"}

  nginx:
    image: nginx:alpine
    container_name: idcd-nginx
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /opt/idcd/nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - /opt/idcd/nginx/ssl:/etc/nginx/ssl:ro
    depends_on:
      api:
        condition: service_healthy
    logging:
      driver: "json-file"
      options: {max-size: "50m", max-file: "5"}
