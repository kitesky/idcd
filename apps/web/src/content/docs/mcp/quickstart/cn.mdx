export const meta = {
  title: 'MCP 快速开始 | idcd 文档',
  description: '5 分钟把 idcd MCP Server 接入 Claude Code 或 Cursor，让 AI 工具调用全球网络拨测能力。',
}

# 5 分钟接入 Claude Code

**步骤 1**：获取 API Key

前往 [idcd 控制台](/app/settings/api-keys) 创建 API Key。

**步骤 2**：安装 MCP server

```bash
go install github.com/kite365/idcd/apps/mcp/cmd/mcp@latest
```

**步骤 3**：添加到 Claude Code

```bash
claude mcp add idcd -- /path/to/mcp --api-key sk_live_xxx
```

或设置环境变量后：

```bash
export IDCD_API_KEY=sk_live_xxx
claude mcp add idcd -- /path/to/mcp
```

**步骤 4**：在对话中使用

```
你: 检测一下 google.com 的全球 Ping 延迟
Claude: [调用 ping tool]
PING google.com
节点: Tokyo JP — 32ms ✓
节点: Singapore SG — 45ms ✓
平均: 38ms | 丢包: 0%
```

## Cursor IDE 集成

在 Cursor 设置中添加 MCP server 配置：

```json
{
  "mcpServers": {
    "idcd": {
      "command": "/path/to/mcp",
      "args": ["--api-key", "sk_live_xxx"]
    }
  }
}
```

## 下一步

- [认证配置](/docs/mcp/authentication) — API Key 管理和权限控制
- [工具列表](/docs/mcp/tools) — 所有可用工具详细说明
